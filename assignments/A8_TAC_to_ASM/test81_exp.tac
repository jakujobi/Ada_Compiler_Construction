proc one
    _t1 = 10      ; Temp for immediate value
    A = _t1       ; Assign to global A
    _t2 = 40      ; Temp for immediate value
    B = _t2       ; Assign to global B
    _t3 = A + B   ; Temp for A + B
    CC = _t3      ; Assign result to global CC
endp one
start proc one
